interface:
  display_name: "CrossFrame Review"
  short_description: "显式点名调用的 CrossFrame 输出评审与修复入口。"
  default_prompt: "Use $crossframe-review only after the user explicitly invokes it."
policy:
  allow_implicit_invocation: false