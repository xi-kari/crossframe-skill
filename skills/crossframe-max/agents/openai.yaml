default_prompt: >
  使用 crossframe-max。先读取 SKILL.md，再读取 protocols/max-worldview-protocol.md。
  以 SKILL.md、protocols、templates 和 scripts 为准，不得用本 prompt 替代协议文件。
  先确定 max-complete、max-design-review 或 max-incomplete/progress。
  普通运行保持 artifact-first、phase-lock、route-ledger 和 longform-dominance 优先。