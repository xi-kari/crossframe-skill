display_name: CrossFrame Suite
short_description: Route complex CrossFrame tasks through the right sequence of sibling skills.
default_prompt: Use CrossFrame Suite to choose the right CrossFrame workflow, read only the needed sibling skills, and produce a short dispatch outline before the final output.
