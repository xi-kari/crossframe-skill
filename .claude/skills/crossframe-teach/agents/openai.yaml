interface:
  display_name: "CrossFrame Teach"
  short_description: "显式点名调用的 CrossFrame 概念教学入口。"
  default_prompt: "Use $crossframe-teach only after the user explicitly invokes it."
policy:
  allow_implicit_invocation: false