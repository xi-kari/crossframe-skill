interface:
  display_name: "CrossFrame Notebook"
  short_description: "显式点名调用的 CrossFrame 读书、理论和文章研究笔记入口。"
  default_prompt: "Use $crossframe-notebook only after the user explicitly invokes it."
policy:
  allow_implicit_invocation: false