# /crossframe-public

Use CrossFrame Public for public issues, platform appeals, institutional governance, policy, or compliance materials.

Read `skills/crossframe-public/SKILL.md`, then follow its routed instructions. Source-check public/current claims or explicitly downgrade.

User input:

`$ARGUMENTS`
