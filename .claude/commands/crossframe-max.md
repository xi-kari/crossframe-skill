# /crossframe-max

Use CrossFrame Max only when the user explicitly asks for maximum CrossFrame mode, exhaustive structural projection, full-scale world-model interpretation, or no word-limit complete explanation.

This explicit command is allowed as an independent mode. Read `skills/crossframe-max/SKILL.md`, then follow its max worldview protocol. Do not route through the `crossframe-suite` `2+1` selector or the ordinary article-type selector. Treat the object as a local world, build `max-worldview-capsule`, `max-source-frontier`, `max-transcendence-window`, `max-continuation-ledger`, `max-red-team-pass`, `max-position-matrix`, `max-path-confidence-layers`, `max-unexhaustible-declaration`, `max-output-layers`, `max-continuation-index`, `max-local-world-model`, and `max-path-tree`, then output `max-dossier` and `max-essay` with evidence boundaries. Enforce the template-fidelity gate: templates are contracts, not references, and generated artifacts should pass `scripts/check_crossframe_max_artifacts.py`.

User input:

`$ARGUMENTS`
