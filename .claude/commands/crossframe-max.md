# /crossframe-max

Use CrossFrame Max only when the user explicitly asks for maximum CrossFrame mode, exhaustive structural projection, full-scale world-model interpretation, or no word-limit complete explanation.

This explicit command is allowed as an independent mode. Read `skills/crossframe-max/SKILL.md`, then follow its max worldview protocol. Do not route through the `crossframe-suite` `2+1` selector or the ordinary article-type selector. Treat the object as a local world, build `max-worldview-capsule`, `max-source-frontier`, `max-transcendence-window`, `max-continuation-ledger`, `max-red-team-pass`, `max-position-matrix`, `max-path-confidence-layers`, `max-unexhaustible-declaration`, `max-output-layers`, `max-continuation-index`, `max-local-world-model`, and `max-path-tree`. Enforce the artifact-first gate: write a durable artifact directory containing `max-artifact-manifest.md`, `max-dossier.md`, `max-essay.md`, `max-continuation-ledger.md`, and `max-continuation-index.md` before final chat. The complete article must live in its own `max-essay.md` file. Final chat gives the artifact index, validation result, short reading guide, continuation entry, and visible "branches we can keep discussing"; do not hide continuation branches only inside files. Enforce the template-fidelity gate: templates are contracts, not references. Enforce the longform-dominance gate: `max-essay` is the final complete explanation, not a thin summary of `max-dossier`. Generated artifacts must pass `scripts/check_crossframe_max_artifacts.py`.

User input:

`$ARGUMENTS`
