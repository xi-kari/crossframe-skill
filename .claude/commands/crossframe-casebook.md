# /crossframe-casebook

Use CrossFrame Casebook to turn materials into reusable casebook entries.

Read `skills/crossframe-casebook/SKILL.md`, then follow its routed instructions. Preserve source boundaries and redact private details.

User input:

`$ARGUMENTS`
