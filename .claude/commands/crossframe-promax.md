# /crossframe-promax

`PROMAX-NAMED-ONLY`：只有 `crossframe-promax`、`CrossFrame ProMax`、`$crossframe-promax`、`/crossframe-promax` 四种明确名称具有触发权。

`PROMAX-PRIORITY-OVER-MAX`：若输入同时点名 ProMax 与 Max，选择 ProMax，并把冲突交给 ProMax run contract 记录。

`PROMAX-NO-FALLBACK-TO-MAX`：进入后始终执行 ProMax 独立 v8 artifact workflow；材料、能力或验证缺口只影响 ProMax 自身状态。

读取唯一 canonical 入口 `skills/crossframe-promax/SKILL.md`，把下列原始用户输入完整传给其确定性路由和生产 `init -> prepare -> materialize -> checker` 流程。不要启动 suite 的模式/角色/文章选择器，不要串接 sibling workflow。

User input:

`$ARGUMENTS`
