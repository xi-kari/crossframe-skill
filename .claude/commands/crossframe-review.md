# /crossframe-review

Use CrossFrame Review to audit a CrossFrame-style output.

Read `skills/crossframe-review/SKILL.md`, then follow its routed instructions. Output review object, fact boundary, triggered rules, grade, key failures, evidence locations, repair steps, and pass/fail.

User input:

`$ARGUMENTS`
