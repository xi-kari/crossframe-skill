# /crossframe

使用 CrossFrame 对用户给出的对象做跨尺度结构诊断。

执行要求：

1. 读取 `skills/crossframe/SKILL.md`。
2. 判断任务类型：诊断、推演、开放断言、低条件行动或高责任审查。
3. 先给简短推理提纲：诊断对象、事实边界、尺度窗口、机制候选、判断档位、下一步。
4. 再输出面向用户的人话判断。
5. 若证据不足，降低判断档位，不要伪装成强判断。

用户输入：

`$ARGUMENTS`
